{
  "name": "portfolio-emilio-tahara-security-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "pnpm run test:unit && pnpm run test:rules",
    "test:unit": "node --test tests/security-utils.test.mjs",
    "test:rules": "firebase emulators:exec --project portfolio-emilio-security-tests --only firestore \"node --test tests/firestore.rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.2",
    "firebase": "12.17.1",
    "firebase-tools": "15.28.1"
  }
}
